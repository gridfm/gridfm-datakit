name: Upload Python Package to PyPI when a version tag is pushed

on:
  push:
    tags:
      - 'v*'

jobs:
  pypi-publish:
    name: Publish release to PyPI
    runs-on: ubuntu-latest

    if: startsWith(github.ref, 'refs/tags/v')

    environment:
      name: pypi
      url: https://pypi.org/p/gridfm-datakit

    permissions:
      id-token: write

    steps:
      - name: Validate tag is a release version
        run: |
          re='^v[0-9]+\.[0-9]+\.[0-9]+((a|b|rc)[0-9]+)?$'
          if [[ ! "$GITHUB_REF_NAME" =~ $re ]]; then
            echo "::error::Tag '$GITHUB_REF_NAME' is not a release version (expected vMAJOR.MINOR.PATCH with an optional aN/bN/rcN suffix)."
            exit 1
          fi
          echo "Tag '$GITHUB_REF_NAME' is a valid release version."

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Ensure tag is on main
        run: |
          git fetch origin main
          if ! git branch --remotes --contains "$GITHUB_SHA" | grep origin/main; then
            echo "Tag is not on main branch. Exiting."
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip wheel build
          pip install -e .[dev,test]

      - name: Build package
        run: |
          python -m build

      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
